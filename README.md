## Vault

**This smart contract implements an ERC-4626 crypto index vault which allows user deposit USDC tokens that is swapped to these five token (WETH, WBTC, LINK, wSOL and PAXG) at a particular allocation. These tokens are rebalanced automaitcally using chainlink automation when the allocatioin drifts ±5% from the original allocation**

